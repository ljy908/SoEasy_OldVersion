


















































































































































































































		}
		else
#endif
	}
	
	if(left == 0){
		gpioMotorWrite = 0;
		mraa_pwm_write(gpioMotor6, gpioMotorWrite);
		
	}
	else
		gpioMotorWrite = 0.05f;
		mraa_pwm_write(gpioMotor6, gpioMotorWrite);
 	}
}
void SettingPwm(mraa_pwm_context* pwmcontext){
	mraa_pwm_period_us(*pwmContext, 200);
	mraa_pwm_enable(*pwmContext, 1);
}






















